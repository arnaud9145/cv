import React from 'react'
import Button from './Button'

import './styles/header.css'

const Header = props => (
    <header>
        <Button>
            test
        </Button>
        <Button selected>
            test
        </Button>
    </header>)

export default Header
